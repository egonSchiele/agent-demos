@README.md

## pnpm vs npm

Please use pnpm instead of npm. Also, when you need to install a package, simply install it like this:

```
pnpm add <package-name>
```

instead of first adding the package and a hard-coded version to package.json and then running pnpm install.